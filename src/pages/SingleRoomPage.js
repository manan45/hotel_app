import React from 'react';

// define the class based component of the room page
class SingleRoom extends React.Component{
    // JSX render function
    render() {
        return(
            <div>
                 Inside single room page
            </div>
        );
    }
}

export default SingleRoom;